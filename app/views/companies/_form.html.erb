<%= simple_form_for(@company) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :name %>
    <%= f.input :location %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
